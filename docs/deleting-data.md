---
id: deleting-data
title: Deleting data
---

To delete a resource server-side, use the `DELETE` HTTP verb. The route you should use is `/:your-model-name/:your-resource-id`.

There shouldn't be a request body.

If the request is successful, you'll receive a `200` response code.